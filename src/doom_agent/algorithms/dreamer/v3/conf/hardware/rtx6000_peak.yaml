# @package _global_
# RTX 6000 Ada / 96GB PRO Peak Performance Settings
# Optimized for 32+ CPU cores and 48GB+ VRAM
agent:
  n_envs: 64
  batch_size: 256
  batch_length: 64
  train_every: 32      # Corregido: 1 actualización cada 32 pasos (Ratio 512:1)
  train_steps: 1
  prefill_steps: 50000
  precision: 16
  compile: true
